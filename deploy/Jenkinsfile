pipeline {
    agent {
        docker 'node:latest'
    }

    stages {
        stage('Build') {
            steps {
                sh 'node --version'
            }
        }
        stage('Deploy') {
            steps {
                
            }
        }
    }
}